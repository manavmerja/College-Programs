#include<iostream>
#include<math.h>
using namespace std;
int main(){
    float number;
    cout<<"Enter the number: ";
    cin>>number;
    float sqrt_number=sqrt(number);
    cout<<"The square root of the number is: "<<sqrt_number;
    return 0;
}